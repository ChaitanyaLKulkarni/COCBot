import React from "react";

export default function P5PlayerSummary() {
    // Drop down selector to choose player (let them type to search)
    // Show Total matches played and mode wise played in graph
    // Liked stacked bargraph maybe?
    // At the end all add total bar and put the label at different side
    // Show wins and win ration w.r.t. modes also (maybe add drop down to choose mode)
    // Bit of languages summary:
    //  1. Favorite languages
    //  2. Different Languages Used
    return <div></div>;
}
